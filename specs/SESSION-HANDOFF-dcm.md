# Handoff — describe-critique-methodology (post plan-18)

> Scratch handoff for a compacted session. Delete once acted on.

## Where things stand

Plan 18 is **complete and retired** (`specs/plans/completed/18-methodology-pipeline-skills.md`
carries full markup + deviation notes). Branch `feat/describe-critique-methodology`,
6 commits, working tree clean, **nothing pushed or merged** — integration is
the user's call (finishing-a-development-branch menu was presented, unanswered).

Tests green: 18 skill tests, 34 build tests. Both lints green.

Deployed: `skillListingBudgetFraction: 0.025` in `~/.claude/settings.json`;
symlink `~/.claude/skills/describe-critique-methodology` → repo. Skill is
live (it appeared in this session's own listing).

## Two gates still open (need a human)

1. **Task 7 Step 4** — review/commit `/Users/lowell/Projects/alt-nfp/specs/nfp-model-methodology.md`
   (generated by the verification run, uncommitted). Was leg 1 of the
   round-trip gating plan #2.
2. **Task 9 Step 3** — `/context` in a NEW session: confirm the skill is
   listed AND no existing personal skill was evicted. Keep the branch
   checked out or the symlink dangles.

## NEW (2026-07-26): the user supplied a REAL round-trip

Not the nfp-model one — a different, real one they ran ~30 min prior:

- `/Users/lowell/Projects/alt-nfp/specs/usable_series_methodology.md` (476 lines)
- `/Users/lowell/Projects/alt-nfp/specs/usable_series_methodology_review.md` (118 lines)

**Both untracked in alt-nfp.** The review is a genuine Research-mode SOTA
critique (dense citations: FRB-ADP/Cajner et al., Homebase/Opportunity
Insights, Hyndman tsfeatures, Diebold-Mariano/Clark-West, selective-editing
literature, X-13ARIMA-SEATS, ONS/UNECE quality frameworks). Verdicts per
component: SOUND / SOUND WITH CAVEATS / BELOW SOTA, plus §10 "missing
entirely" (panel-composition churn is called the biggest gap), 9 numbered
Recommendations in must-fix / high-priority / nice-to-have tiers, and direct
answers to the methodology's 4 open questions.

**This closes the spec Req 13 evidence gap** (a real describe→Chat→critique
round-trip exists) and is the missing fixture for the deferred item
"Synthesize mode has no scenario verification."

### Frictions found — real-world, worth recording as findings

These are exactly what a synthetic fixture would never surface:

1. **Naming mismatch breaks mode selection.** The skill selects Synthesize
   mode when `specs/<name>-critique.md` sits beside
   `specs/<name>-methodology.md`. Actual files use underscores and
   `_review` (`usable_series_methodology.md` /
   `usable_series_methodology_review.md`). The rule as written would NOT
   fire. Either loosen the rule (match `*review*` / `*critique*` beside a
   `*methodology*` file) or treat the naming as a convention the handoff
   message must enforce.
2. **No routing header** — the review predates the skill, so it was not
   produced with `references/critique-prompt.md`. MT2 showed the
   frontmatter description alone routes 5/5, so this is survivable, but
   it means the header cannot be assumed present on real inputs.
3. **Critique points are not `C1..Cn`.** They are §1–§10 plus
   Recommendations 1–9. `spec-synthesis.md` says "Number every critique
   point (C1, C2, …) so a downstream triage table can cite them" — the
   triage table needs a locator scheme for un-numbered real critiques
   (proposal: cite `§N` and `R#`).
4. **Adjudication status unknown.** The skill's prompt asks Chat to record
   *adjudicated* positions after interactive push-back. Unknown whether
   the user pushed back or took the first pass. Ask before triaging —
   "reject" verdicts inherit differently depending on the answer.

### Next action (was about to start)

Run **Synthesize mode** on the pair for real: staleness check → triage
table (one batched question set, every §/R point, accept/reject/
needs-user-adjudication) → house-format spec → derive-roadmap routing
header → self-review → user gate. Note derive-roadmap does NOT exist yet,
so the skill's step 7 says: say so and stop; do not improvise a roadmap.

Open question for the user before triage: was the review interactively
adjudicated, or is it a first pass?
